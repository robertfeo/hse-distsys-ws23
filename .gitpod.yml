image:
  file: .gitpod.Dockerfile
tasks:
  - init: |
      sdk install java
      cd backend/
      mvn clean package
      cd ..
      npm install -g npm@10.2.4
      cd frontend/
      npm install --silent
      npm run build
      cd ..
      docker-compose up -d
